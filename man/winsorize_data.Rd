% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/winzorise.r
\name{winsorize_data}
\alias{winsorize_data}
\title{Aplicar Winsorization a variaveis numericas}
\usage{
winsorize_data(dataset = NULL, metadados = NULL, cluster_ref = NULL)
}
\arguments{
\item{dataset}{\code{data.frame} com os dados originais.}

\item{metadados}{\code{data.frame} com metadados das variaveis. Deve conter colunas:
\itemize{
\item \code{Classe} - Tipo da variavel ("Numerico").
\item \code{Code} - Nome da coluna correspondente em \code{dataset}.
}}

\item{cluster_ref}{Vetor ou coluna de cluster_ref para identificar grupos (usado no caso de variaveis "Cluster").}
}
\value{
Um \code{data.frame} com o resumo da Winsorization de cada variavel.
}
\description{
Aplica o processo de Winsorization a colunas numericas
}
\details{
Esta funcao utiliza a funcao auxiliar \code{wins_par} para calcular os limites de Winsorization
e retorna um resumo para cada variavel processada.
\itemize{
\item Se houver variaveis do tipo "Cluster", o processo e aplicado separadamente para cada grupo identificado em \code{cluster_ref}.
\item As variaveis sao retornadas na ordem definida por \code{metadados$Code}.
}
}
\examples{
# Exemplo ficticio (assumindo que wins_par ja esteja implementada)
dataset <- data.frame(
  var1 = c(1, 2, 3, 100),
  var2 = c(10, 20, 30, 40),
  cluster = c(1, 1, 2, 2)
)

metadados <- data.frame(
  Classe = c("Numerico", "Numerico", "Cluster"),
  Code = c("var1", "var2", "cluster")
)

cluster_ref <- dataset$cluster

winsorize_data(dataset, metadados, cluster_ref)

}
